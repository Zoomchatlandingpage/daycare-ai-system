/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    "*.replit.dev",
    "*.spock.replit.dev",
    "127.0.0.1",
    "localhost",
  ],
};

export default nextConfig;
